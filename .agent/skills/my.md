---
name: VPN核心架构，系统架构设计参考
description: VPN核心架构的详细说明。
---

- ## When to use this skill
- 当要调整系统架构设计时，必须参考这里的VPN架构图

## How to use it
- 先检查核心架构图，看看需要改动哪里，然后征求同意后再开发

# VPN核心架构
```text
                ┌───────────────────────────────┐
                │         用户看到的界面         │
                │   (Jetpack Compose / XML)     │
                └───────────────┬───────────────┘
                                │
                                ▼
                ┌───────────────────────────────┐
                │          业务逻辑层           │
                │ 订阅解析 · 节点列表 · 测速结果  │
                │ 规则生成 · 支付回调 · 用户状态  │
                └───────────────┬───────────────┘
                                │           你自己写的这一大块
                                ▼           （已经做好了）
                ┌───────────────────────────────┐
                │     配置生成器 (最关键桥梁)    │
                │   把你的节点+规则+设置 → JSON   │
                │   每次切换/续费/更新订阅都生成  │
                └───────────────┬───────────────┘
                                │
               ┌────────────────┼────────────────┐
               │                                 │
               ▼                                 ▼
┌────────────────────────────┐       ┌────────────────────────────┐
│                            │       │                            │
│  官方 sing-box Android Lib │       │       Android 系统         │
│                            │       │                            │
│  ┌──────────────────────┐  │       │  ┌──────────────────────┐  │
│  │    sing-box 核心引擎  │◄─┼───────┼─►│      VpnService      │  │
│  │ (支持所有协议、规则)  │  │       │  │   (系统级代理通道)    │  │
│  └──────────────────────┘  │       │  └──────────────────────┘  │
│          ▲                 │       │           ▲                 │
│          │                 │       │           │                 │
│  ┌───────────────┐         │       │  ┌───────────────┐         │
│  │   Config JSON  │─────────┘       └─►│    TUN 设备   │◄───────┘
│  └───────────────┘                     │    (虚拟网卡)   │
│                                        └───────────────┘
│            ▲                                       ▲
│            │                                       │
└───────────┼───────────────────────────────────────┘
            │                全部网络流量都走这里
            ▼
       互联网（国内/国外服务器）


